# Data model

Placeholder. Filled in by Phase 1 when the first tables land
(`connected_accounts`, `oauth_tokens`, `sync_cursors`, `emails`,
`email_content_blobs`).

A Mermaid `erDiagram` is the canonical rendering — lint-friendly, visible
inline on GitHub. Per plan §20.2 we do **not** ship the auto-generated ERD
pipeline for 1.0.0; this file is hand-maintained alongside migrations.

```mermaid
erDiagram
  %% to be populated in Phase 1+
```
