# Plans

All implementation plans for Briefed live here. Per [CLAUDE.md](../../CLAUDE.md) §3:

- Name files `YYYY-MM-DD-<slug>.md`.
- One plan per file.
- Keep plans in this directory — never at repo root, `backend/`, or `frontend/`.
