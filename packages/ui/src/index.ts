/**
 * Barrel export for @briefed/ui primitives.
 *
 * Phase 0 ships only the <Motion> helper; Phase 6 adds the full primitive
 * set referenced in plan §19.16.
 */
export { Motion, type MotionProps } from './primitives/Motion';
