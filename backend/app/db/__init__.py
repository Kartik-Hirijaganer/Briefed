"""SQLAlchemy models, sessions, and repositories.

Phase 1 introduces the Gmail-ingestion tables: ``users``,
``connected_accounts``, ``oauth_tokens``, ``sync_cursors``, ``emails``,
and ``email_content_blobs``. Later phases layer classification,
summarization, jobs, and unsubscribe tables on top.
"""
